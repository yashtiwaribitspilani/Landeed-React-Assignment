// src/config/endpoints.js

const BASE_URL = "https://landeedbackend.onrender.com/api";

const endpoints = {
  formConfig: `${BASE_URL}/formConfig`,
  submitForm: `${BASE_URL}/submitForm`,
};

export default endpoints;
